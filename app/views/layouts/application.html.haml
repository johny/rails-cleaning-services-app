!!!
%html
  %head
    %title
      Kraclean - Profesjonalne sprzątanie mieszkań i biur w Krakowie
    %meta{:name => "description", :content => "Twoje mieszkanie będzie ślnić!. Sprawnie i rzetelnie posprzątamy dom, mieszkanie i biuro - jesteśmy do usług. Zadbamy o czystość i porządek w Twoim domu."}
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body{class: body_class}
    - if Rails.env == 'production'
      = render partial: 'layouts/gat'
    - if content_for? :intro
      = render partial: 'layouts/simple_header'
      = yield :intro
    - else
      = render partial: 'layouts/header'
      %div.container
        - if content_for? :single
          %div.row
            %div.col-md-6.col-md-offset-3
              = render partial: 'layouts/flash'
              = yield :single
        - else
          %div.row
            %div.col-md-12
              = render partial: 'layouts/flash'
              = yield

    = render partial: 'layouts/footer'
